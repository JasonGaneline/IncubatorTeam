# Copy to `.env` and adjust values. The app loads these via pydantic-settings (see app/core/config.py).

DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.wxbfcjobiywwiqpaqrwd.supabase.co:5432/postgres

# openssl rand -hex 32
SECRET_KEY=replace-with-a-long-random-secret

JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Optional: comma-separated origins for browser clients (Vite default below)
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
